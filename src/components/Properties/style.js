import styled from "styled-components";

const Container = styled.div`
    display: flex;
    flex-wrap: wrap;
    padding: var(--padding);
    justify-content: space-between ;
    gap: 20px;

`

export {Container}